---
name: Bug report
about: Something misbehaved — an adapter, a policy engine, the CLI
labels: bug
---

**What happened / what you expected**

**Trace record** (sanitized — one JSONL line from `traces/` usually suffices;
run `python scripts/sanitize_traces.py` or redact ids/balances by hand)

```json
```

**Environment**: gpuhedge version, Python, provider + region (if live)
