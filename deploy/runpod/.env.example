# Build-time env for `flash deploy` (copied into the worker ENV in moss_worker.py).
GPUHEDGE_S3_BUCKET=your-bucket
GPUHEDGE_VOICE_PREFIX=voices
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
